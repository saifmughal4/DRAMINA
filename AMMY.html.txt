<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Doctor Amina • Magic Notes</title>

<style>
html,body{
  margin:0; padding:0; height:100%;
  font-family:"Segoe UI", Arial, sans-serif;
  background:radial-gradient(circle at top,#ff8acb,#4a163f);
  overflow:hidden;
}

#loginScreen,#magicScreen{
  display:flex;
  justify-content:center;
  align-items:center;
  flex-direction:column;
  width:100%; height:100%;
  text-align:center;
  color:#ffd6ec;
}

.box{
  background:#120013;
  border-radius:22px;
  padding:25px;
  width:90%;
  max-width:420px;
  box-shadow:0 0 45px rgba(255,105,180,.6);
}

h1{margin-bottom:20px;}

input{
  width:100%;
  padding:14px;
  font-size:17px;
  border-radius:16px;
  border:2px solid #ff6fb5;
  background:black;
  color:white;
  outline:none;
}

button{
  margin-top:18px;
  padding:14px 30px;
  font-size:18px;
  border:none;
  border-radius:18px;
  background:#ff6fb5;
  color:black;
  cursor:pointer;
}

#magicScreen{display:none; position:relative;}

.note{
  font-size:18px;
  line-height:1.7;
  color:#ffd6ec;
  margin-top:10px;
}

.signature{
  margin-top:18px;
  font-style:italic;
  color:#ff9ad5;
}

.back{
  margin-top:25px;
}

.heart,.balloon,.sparkle{
  position:absolute;
  pointer-events:none;
}

.heart{
  color:#ff66a3;
  animation:float 6s linear infinite;
}

.balloon{
  font-size:40px;
  animation:floatBalloon 12s linear infinite;
}

.sparkle{
  width:4px;height:4px;
  background:#ff9ad5;
  border-radius:50%;
  opacity:.7;
  animation:sparkle 2s infinite;
}

@keyframes float{
  0%{transform:translateY(0) scale(1);}
  100%{transform:translateY(-600px) scale(1.4);}
}
@keyframes floatBalloon{
  0%{transform:translateY(0);}
  100%{transform:translateY(-900px);}
}
@keyframes sparkle{
  0%,100%{opacity:.8;}
  50%{opacity:.2;}
}
</style>
</head>

<body>

<!-- LOGIN -->
<div id="loginScreen">
  <div class="box">
    <h1>Secure Access • Doctor Amina ✨</h1>
    <input type="password" id="passwordInput" placeholder="Enter password">
    <button onclick="unlock()">Unlock</button>
    <p id="msg" style="color:#ff9ad5;margin-top:10px;"></p>
  </div>
</div>

<!-- MAGIC PAGE -->
<div id="magicScreen">
  <div class="box">
    <h1>🌸 A Quiet Note 🌸</h1>

    <div class="note" id="noteText"></div>

    <div class="signature">— Proud of you, always</div>

    <button class="back" onclick="location.reload()">Lock Again 💖</button>
  </div>
</div>

<audio id="bgm" loop>
  <source src="https://cdn.pixabay.com/audio/2022/10/14/audio_9b26d5b21c.mp3" type="audio/mpeg">
</audio>

<script>
const PASSWORD="saif u r handsome";

const notes=[
`You walked in with books and restless dreams,<br>
a student learning how hope feels.<br><br>
You walked out wearing purpose and calm,<br>
healing lives with steady palms.<br><br>
You came as a student,<br>
you returned as a doctor —<br>
and somewhere between pages and prayers,<br>
you became my cutie.`,

`Not everyone who studies becomes a healer,<br>
but you learned kindness before knowledge.<br><br>
White coat on your shoulders,<br>
soft heart in your chest —<br>
Doctor Amina, you were always meant for this.`,

`This isn’t just a degree,<br>
it’s proof of patience, tears, dua, and courage.<br><br>
You didn’t just pass exams,<br>
you became someone the world needs.`
];

let noteIndex=0;

function unlock(){
  const input=document.getElementById("passwordInput").value.trim();
  if(input===PASSWORD){
    document.getElementById("loginScreen").style.display="none";
    document.getElementById("magicScreen").style.display="flex";
    document.getElementById("bgm").play();
    showNotes();
    magic();
  }else{
    document.getElementById("msg").innerText="Wrong password ❌";
  }
}

function showNotes(){
  const el=document.getElementById("noteText");
  el.innerHTML=notes[noteIndex];
  setInterval(()=>{
    noteIndex=(noteIndex+1)%notes.length;
    el.innerHTML=notes[noteIndex];
  },6000);
}

function magic(){
  for(let i=0;i<25;i++){
    let h=document.createElement("div");
    h.className="heart";
    h.innerHTML="❤";
    h.style.left=Math.random()*innerWidth+"px";
    h.style.top=innerHeight+"px";
    h.style.fontSize=15+Math.random()*25+"px";
    document.body.appendChild(h);
  }

  for(let i=0;i<10;i++){
    let b=document.createElement("div");
    b.className="balloon";
    b.innerHTML="🎈";
    b.style.left=Math.random()*innerWidth+"px";
    b.style.top=innerHeight+"px";
    document.body.appendChild(b);
  }

  for(let i=0;i<40;i++){
    let s=document.createElement("div");
    s.className="sparkle";
    s.style.left=Math.random()*innerWidth+"px";
    s.style.top=Math.random()*innerHeight+"px";
    document.body.appendChild(s);
  }
}
</script>

</body>
</html>
